import { TitleConverterPipe } from './title-converter.pipe';

describe('TitleConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
